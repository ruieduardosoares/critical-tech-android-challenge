// Project properties accessed by all sub projects
ext {
    // Config shared to all modules
    compileSdkVersion = 33
    minSdkVersion = 23
    targetSdkVersion = 33
    buildToolsVersion = "33.0.0"
    applicationId = "com.topnews"
    versionName = "0.1"
    versionCode = 1
    coreLibraryDesugaringEnabled = true
    javaJvmTarget = JavaVersion.VERSION_11
    kotlinJvmTarget = "11"
    minify = true
    debugDebuggable = true

    // Java 8+ compatibility
    desugarJdkLibsVersion = "1.1.5"

    // Kotlin
    kotlinVersion = "1.7.10"

    // DI
    hiltVersion = "2.42"

    // UI
    androidxCoreKtxVersion = "1.9.0"
    androidxActivityKtxVersion = "1.6.1"
    androidxAppCompatVersion = "1.5.1"
    androidxConstraintLayoutVersion = "2.1.4"
    androidxAnnotationVersion = "1.5.0"
    googleMaterialVersion = "1.7.0"
    pagingVersion = "3.1.1"
    coilVersion = "2.2.2"

    //Biometric
    androidXBiometricVersion = "1.2.0-alpha05"

    //Navigation
    androidXNavigationVersion = "2.5.3"

    // AndroidX Lifecycle Commons
    lifecyleJava8CommonsVersion = "2.5.1"

    // Logs
    timberVersion = "4.7.1"

    // Core
    coroutinesAndroidVersion = "1.6.4"

    // Http/s client
    retrofitVersion = "2.9.0"
    moshiVersion = "1.13.0"
    mockWebServer = "4.10.0"

    // Mocking
    mockitoVersion = "4.6.1"
    mockitoAndroidVersion = "4.6.1"
    mockito2KotlinVersion = "4.0.0"

    // MockK to mock extension functions
    mockkVersion = "1.12.2"

    // Jvm Tests
    junitBomVersion = "5.8.2"
    kluentVersion = "1.68"

    // AndroidX
    androidXTestCoreVersion = "1.5.0"
    androidXTestVersion = "1.5.0"
    androidXExtJunitVersion = "1.1.4"
    androidXTestOrchestrator = "1.3.0"
}
